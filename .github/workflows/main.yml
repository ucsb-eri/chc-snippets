---
name: 'deploy to Dokku'

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Cloning repo
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Push to dokku
        uses: dokku/github-action@master
        with:
          git_remote_url: 'ssh://dokku@grit-web01.grit.ucsb.edu:22/chc-snippets'
          ssh_private_key: ${{ secrets.SSH_PRIVATE_KEY }}
