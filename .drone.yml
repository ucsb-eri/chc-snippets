kind: pipeline
type: docker
name: build-and-save

steps:
  - name: build
    image: docker
    commands:
      - docker build -t chc-snippets .
    volumes:
      - name: dockersock
        path: /var/run/docker.sock

volumes:
  - name: dockersock
    host:
      path: /var/run/docker.sock
